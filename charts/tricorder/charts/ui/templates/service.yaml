apiVersion: v1
kind: Service
metadata:
  name: {{ include "tricorder.fullname" . }}-tricorder-ui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tricorder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: {{ include "tricorder.ui.svc.ports" . | nindent 4 }}
  selector:
    {{- include "tricorder.selectorLabels" . | nindent 4 }}
